services:
  frontend:
    build: ./frontend
    container_name: supfile-frontend-client
    ports:
      - "5173:5173"
    env_file:
      - ./frontend/.env
    environment:
      - CHOKIDAR_USEPOLLING=false
      - WATCHPACK_POLLING=false
    volumes:
      - ./frontend/src:/app/src
      - /app/node_modules
    restart: unless-stopped